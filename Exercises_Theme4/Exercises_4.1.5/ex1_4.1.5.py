# Implement a solution that turns all the lower case words into upper case words

vehicles = ['aviao', 'carro', 'navio', 'onibus']

new_list = map(lambda vehicle: vehicle.upper(), vehicles)
print(list(new_list))
